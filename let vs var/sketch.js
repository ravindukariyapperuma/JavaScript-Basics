function setup(){
    var i, i, x, y;
    console.log(y);

    for (i = 0; i < 100; i++){
        //some stuff happen
    }
    for (i = 0; i < 100; i++){
        //some stuff happen
    }

    x = mouseX + 100;
    if (x > 50) {
        y = 20;
        ellipse(x, y, 100, 100);
    }
    console.log(i);
}